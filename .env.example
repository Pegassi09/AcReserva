# MongoDB Atlas connection string. Keep this secret outside version control.
MONGODB_URI=mongodb+srv://USER:PASSWORD@CLUSTER.mongodb.net/?retryWrites=true&w=majority
MONGODB_DB=ac_reserva
# Generate a long random value: openssl rand -hex 32
JWT_SECRET=replace-with-a-long-random-secret
# Frontend domain, without trailing slash. Comma-separate if needed.
ALLOWED_ORIGINS=https://seu-projeto.vercel.app
# Optional: Google Calendar integration is enabled only when all values are set.
GOOGLE_CALENDAR_ID=
GOOGLE_SERVICE_ACCOUNT_JSON=
